apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kube-system

# Vendored from https://github.com/kubernetes-sigs/descheduler/tree/v0.35.1/kubernetes
# Runs as a long-lived Deployment that re-evaluates every 5 min.
resources:
  - rbac.yaml
  - deployment.yaml
  - policy-configmap.yaml
