apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: runner-state
  namespace: github-actions
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: nfs-csi
  resources:
    requests:
      storage: 5Gi
